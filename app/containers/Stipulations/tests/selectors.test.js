// import { fromJS } from 'immutable'
// import { makeSelectStipulationsDomain } from '../selectors'

// const selector = makeSelectStipulationsDomain()

describe('makeSelectStipulationsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
