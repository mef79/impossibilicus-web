// import { fromJS } from 'immutable'
// import { makeSelectVariableDialogDomain } from '../selectors'

// const selector = makeSelectVariableDialogDomain()

describe('makeSelectVariableDialogDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
