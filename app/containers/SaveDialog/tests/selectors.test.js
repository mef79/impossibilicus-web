// import { fromJS } from 'immutable'
// import { makeSelectSaveDialog2Domain } from '../selectors'

// const selector = makeSelectSaveDialog2Domain()

describe('makeSelectSaveDialog2Domain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
