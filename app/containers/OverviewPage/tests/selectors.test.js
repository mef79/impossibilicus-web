// import { fromJS } from 'immutable'
// import { makeSelectOverviewPageDomain } from '../selectors'

// const selector = makeSelectOverviewPageDomain()

describe('makeSelectOverviewPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
