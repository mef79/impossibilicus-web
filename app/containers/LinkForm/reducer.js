/*
 *
 * LinkForm reducer
 *
 */

import { fromJS } from 'immutable'

const initialState = fromJS({})

function linkFormReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export default linkFormReducer
