// import { fromJS } from 'immutable'
// import { makeSelectGraphDomain } from '../selectors'

// const selector = makeSelectGraphDomain()

describe('makeSelectGraphDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
