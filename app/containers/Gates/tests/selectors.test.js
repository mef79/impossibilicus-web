// import { fromJS } from 'immutable'
// import { makeSelectGatesDomain } from '../selectors'

// const selector = makeSelectGatesDomain()

describe('makeSelectGatesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
