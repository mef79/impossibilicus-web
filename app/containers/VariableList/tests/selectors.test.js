// import { fromJS } from 'immutable'
// import { makeSelectVariableListDomain } from '../selectors'

// const selector = makeSelectVariableListDomain()

describe('makeSelectVariableListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
